import { PaginatedEntry } from "../";
export interface ConfirmationServices extends PaginatedEntry {
    end: string;
    start: string;
    bank: string;
    validator: string;
}
export declare type BankConfirmationServices = Omit<ConfirmationServices, "validator">;
export declare type ValidatorConfirmationServices = Omit<ConfirmationServices, "bank">;

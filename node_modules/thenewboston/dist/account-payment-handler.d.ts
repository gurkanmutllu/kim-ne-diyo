import type { Account } from "./account";
import type { AccountPaymentHandlerOptions, Transaction } from "./models";
export declare class AccountPaymentHandler {
    private client;
    private account;
    constructor({ account, bankUrl }: AccountPaymentHandlerOptions);
    init(): Promise<void>;
    sendCoins(recipient: Account | string, amount: number, memo?: string): Promise<void>;
    sendBulkTransactions(transactions: Transaction[]): Promise<void>;
}

import type { AccountData } from "../models";
/**
 * Generates an account with a 32-bit account number and signing key.
 * @returns the generated account
 */
export declare function createAccountData(): AccountData;

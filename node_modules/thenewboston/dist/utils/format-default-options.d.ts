import type { ServerNodeOptions } from "../models";
/**
 * Formats the options for server nodes.
 * @param options the object for the server node options
 */
export declare function formatDefaultOptions(options: {
    [key: string]: any;
}): ServerNodeOptions;

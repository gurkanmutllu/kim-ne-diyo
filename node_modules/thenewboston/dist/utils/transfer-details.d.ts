import type { Account } from "../account";
export declare class TransferDetails {
    sender: Account;
    recipient: Account | string;
    amount: number;
    memo: string;
    constructor(sender: Account, recipient: Account | string, amount: number, memo: string);
}
